void main(){
  List<int>ls=[1,2,3,4,5];
  // ls.add(6);
  // ls.addAll([6,7,8]);
  // ls.remove(3);
  // print(ls.isNotEmpty);
  // print(ls.length);
  // print(ls.first);
  // print(ls.reversed.toList());
  // print(ls.lastOrNull);

}